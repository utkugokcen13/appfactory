[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "appfactory"
version = "0.1.0"
description = "Automated mobile app production pipeline powered by Claude."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["factory*"]
